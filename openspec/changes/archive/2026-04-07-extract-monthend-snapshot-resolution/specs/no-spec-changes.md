# Spec Changes

This change is a pure internal refactor. No domain behavior is added, removed, or modified.

The following capabilities are touched in implementation only, and their specs remain unchanged:
- `monthend-status-overview`
- `monthend-task-worklist`
