# Spec Changes

This change is a pure infrastructure refactor. No domain behavior is added, removed, or modified.

The following capabilities are touched in implementation only — their specs remain unchanged:
- `user-aggregate`
- `project-aggregate`
- `user-sync`
- `project-sync`
